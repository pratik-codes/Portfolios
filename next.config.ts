import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["pbs.twimg.com", "avatars.githubusercontent.com", "fonts.googleapis.com"],
  },
};

export default nextConfig;
